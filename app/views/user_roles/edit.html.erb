<% if @u_roles.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@u_roles.errors.count, "error") %> prohibited
        this u_roles from being saved:
      </h2>
      <ul>
        <% @u_roles.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
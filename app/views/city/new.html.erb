<body bgcolor="OOCC99"></body>
<%= form_with scope: :city, url: city_index_path, local: true do |form| %>

<% if @city.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@city.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @city.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  
  <p>
    <%= form.label :city_name %><br>
    <%= form.text_field :city_name %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>

  
<%#= link_to 'me', geeta_path%>
 
<%= link_to 'List cities', city_index_path %>
<% end %>

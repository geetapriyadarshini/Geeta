<body bgcolor="OOCC99"></body>
<%= form_with scope: :user_region, url: user_regions_path, local: true do |form| %>

<% if @u_region.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@u_region.errors.count, "error") %> prohibited
        this u_region from being saved:
      </h2>
      <ul>
        <% @u_region.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  
  <p>
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.order(:email),:id,:email, include_blank: true %>
</p>


<p>
     <%= form.label :region_id %>
       <%= form.collection_select :region_id, Region.order(:region_name),:id,:region_name, include_blank: true %>
  </p>
 
 
  <p>
    <%= form.submit %>
  </p>

  
<%#= link_to 'me', geeta_path%>
 
<%= link_to 'List nodes', user_regions_path %>
<% end %>

<%= form_for @img, html: { multipart: true } do |f| %>
  <% if @img.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@img.errors.count, "error") %> prohibited this image from being saved:</h2>
 
      <ul>
      <% @img.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="field">
    <%= f.label :real_name %><br>
    <%= f.text_field :real_name %>
  </div>
  <div class="field">
    <%= f.label :image_name %><br>
    <%= f.file_field :image_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>



